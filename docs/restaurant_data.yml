Testing API for location's nearby restaurant data.
---
parameters:
  - name: keyword
    in: path
    type: string
    enum: ["火鍋"]
    required: true
  - name: loc
    in: path
    type: string
    enum: ["25.018720,121.536800"]
    required: true
responses:
  200:
    description: location's nearby restaurant data
    examples:
      rgb: ["red", "green", "blue"]
