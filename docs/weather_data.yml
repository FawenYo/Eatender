Testing API for location's weather data.
---
parameters:
  - name: lat
    in: path
    type: string
    enum: ["25.018720"]
    required: true
  - name: lng
    in: path
    type: string
    enum: ["121.536800"]
    required: true
responses:
  200:
    description: location's weather data
    examples:
      rgb: ["red", "green", "blue"]
