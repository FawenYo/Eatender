version: "3.7"

services:
  project:
    build: ./project
    container_name: eatender
    expose:
      - 8001
    environment:
      - TZ=Asia/Taipei
  nginx:
    build: ./nginx
    container_name: template_nginx
    # restart: always
    links:
      - project
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - project
